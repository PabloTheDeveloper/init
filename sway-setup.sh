#!/bin/bash
# Install sway related projects
sudo apt-get install sway \
	swaylock

# Install .config projects
sudo apt-get install kanshi
# app launcher
sudo apt-get install wofi 
# nightshift mode
sudo apt-get install wlsunset



# status bar
sudo apt-get install i3blocks
# Install apps for status bar
sudo apt-get install acpi
